name: 🤖 Bot Automatizado de Notícias

on:
  schedule:
    # Executa a cada 2 horas
    - cron: '0 */2 * * *'
  # Permite execução manual
  workflow_dispatch:

jobs:
  process-news:
    runs-on: ubuntu-latest
    name: Processar e Publicar Notícias
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4
      
      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 Instalar dependências Python
        working-directory: ./bot
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 🤖 Executar bot de notícias
        working-directory: ./bot
        env:
          API_URL: ${{ secrets.API_URL || 'https://portalnorte43.com.br' }}
          API_KEY: ${{ secrets.API_KEY || 'portal-norte-43-auto-2025' }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CLAUDE_MODEL: ${{ secrets.CLAUDE_MODEL || 'claude-3-haiku-20240307' }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          MAX_NEWS_PER_RUN: '2'
        run: |
          python news_automation_bot.py
      
      - name: 📤 Commit e push das notícias (se houver)
        if: success()
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/automated-news.json public/images/news/*.png 2>/dev/null || true
          if git diff --staged --quiet; then
            echo "Nenhuma mudança para commitar"
          else
            git commit -m "🤖 Bot: Adicionar notícias automatizadas [skip ci]"
            git push
          fi
      
      - name: ✅ Verificar resultado
        if: always()
        run: |
          echo "✅ Bot executado com sucesso!"
          echo "📰 Verifique as notícias em: https://portalnorte43.com.br"

